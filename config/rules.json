{
  "rules": [
    {
      "id": "os-temp",
      "risk": "caution",
      "safeAfterDays": 7,
      "description": "OS temp can contain installers or in-use files; only clean old items."
    },
    {
      "id": "npm-cache",
      "risk": "safe",
      "safeAfterDays": 14,
      "description": "npm cache is reproducible; safe to clean when old."
    },
    {
      "id": "symlink-test",
      "risk": "safe",
      "safeAfterDays": 0,
      "description": "Symlink silme testi için kural.",
      "paths": ["d:/diskcare/_manual/symlink-test/link.txt"]
    }
  ],
  "defaults": {
    "risk": "caution",
    "safeAfterDays": 30
  }
}
