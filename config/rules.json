{
  "rules": [
    {
      "id": "os-temp",
      "risk": "caution",
      "safeAfterDays": 7,
      "description": "OS temp can contain installers or in-use files; only clean old items."
    },
    {
      "id": "npm-cache",
      "risk": "safe",
      "safeAfterDays": 14,
      "description": "npm cache is reproducible; safe to clean when old."
    },
    {
      "id": "sandbox-cache",
      "risk": "safe",
      "safeAfterDays": 0,
      "description": "Local sandbox cache for testing clean --apply safely."
    }
  ],
  "defaults": {
    "risk": "caution",
    "safeAfterDays": 30
  }
}
